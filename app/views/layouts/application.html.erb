<!DOCTYPE html>
<html class="h-full bg-gray-50">
  <head>
    <title><%= content_for(:title) || "Prompt Editor" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full">
    <div class="min-h-full">
      <nav class="bg-gray-900">
        <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
          <div class="flex h-14 items-center justify-between">
            <div class="flex items-center gap-6">
              <%= link_to root_path, class: "flex items-center gap-2 text-white font-bold text-lg" do %>
                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-indigo-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" />
                </svg>
                Prompt Editor
              <% end %>
              <div class="flex gap-1">
                <%= link_to "프롬프트", prompts_path, class: "text-gray-300 hover:bg-gray-700 hover:text-white rounded-md px-3 py-2 text-sm font-medium #{request.path.start_with?('/prompts') || request.path == '/' ? 'bg-gray-700 text-white' : ''}" %>
                <%= link_to "노트", notes_path, class: "text-gray-300 hover:bg-gray-700 hover:text-white rounded-md px-3 py-2 text-sm font-medium #{request.path.start_with?('/notes') ? 'bg-gray-700 text-white' : ''}" %>
                <%= link_to "카테고리", categories_path, class: "text-gray-300 hover:bg-gray-700 hover:text-white rounded-md px-3 py-2 text-sm font-medium #{request.path.start_with?('/categories') ? 'bg-gray-700 text-white' : ''}" %>
                <%= link_to "AI 설정", ai_providers_path, class: "text-gray-300 hover:bg-gray-700 hover:text-white rounded-md px-3 py-2 text-sm font-medium #{request.path.start_with?('/ai_providers') ? 'bg-gray-700 text-white' : ''}" %>
                <%= link_to "실행 기록", executions_path, class: "text-gray-300 hover:bg-gray-700 hover:text-white rounded-md px-3 py-2 text-sm font-medium #{request.path.start_with?('/executions') ? 'bg-gray-700 text-white' : ''}" %>
              </div>
            </div>
          </div>
        </div>
      </nav>

      <% if notice.present? %>
        <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 mt-4">
          <div class="rounded-md bg-green-50 p-3 border border-green-200" data-controller="dismissable">
            <div class="flex items-center justify-between">
              <p class="text-sm text-green-700"><%= notice %></p>
              <button data-action="click->dismissable#dismiss" class="text-green-500 hover:text-green-700">
                <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
              </button>
            </div>
          </div>
        </div>
      <% end %>

      <% if alert.present? %>
        <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 mt-4">
          <div class="rounded-md bg-red-50 p-3 border border-red-200">
            <p class="text-sm text-red-700"><%= alert %></p>
          </div>
        </div>
      <% end %>

      <main class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-6">
        <%= yield %>
      </main>
    </div>
  </body>
</html>
